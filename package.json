{
  "name": "deep-tree",
  "private": true,
  "version": "0.0.0",
  "description": "Deep Tree — monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "publish:all": "pnpm -r publish --access public"
  },
  "devDependencies": {}
}
